name: Mosaic Detector
external_tasks:
  - name: "Scan Mosaics"
    arguments:
      - name: ReCheck
        type: Boolean
        default: false
    exec:
      - python
      - "{pluginDir}/detect.py"
