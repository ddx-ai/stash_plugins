name: Mosaic Detector
description: 全面モザイクや格子状パターンを自動検出し、タグを付与します。
version: 1.0

# ここから修正
exec:
  - python
  - "{pluginDir}/detect.py"

interface: raw

# 以下の 'requires' ブロックを削除してください
# requires:
#   - PythonDepManager
#   - opencv-python
#   - requests
#   - numpy

tasks:
  - name: Scan All Images for Mosaics
    description: 全画像をスキャンしてモザイクを判定します
