name: Mosaic Detector
description: Detects mosaics in images
version: 1.0

# Taskの枠組みを使わず、直接実行コマンドだけを定義
exec:
  - python
  - "{pluginDir}/detect.py"

tasks:
  - name: "Scan Mosaics"
    description: "モザイク判定を開始します（判定済みの扱いはPy側で設定）"
